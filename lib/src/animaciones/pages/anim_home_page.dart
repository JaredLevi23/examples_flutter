import 'package:flutter/material.dart';

class AnimHomePage extends StatelessWidget {
const AnimHomePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
      
    );
  }
}